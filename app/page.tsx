export default function Home() {
  return (
    <main className="pad" style={{ paddingBlock: 76 }}>
      <p className="kicker">AI Operating System for Leaders</p>
      <h1 style={{ fontSize: "var(--text-hero)", lineHeight: "var(--text-hero--line-height)" }}>
        Scaffold live — design tokens wired up
      </h1>
      <p style={{ fontFamily: "var(--font-mono-stack)", color: "var(--blue-hi)", marginTop: 16 }}>
        Phase 4 replaces this with the ported home page.
      </p>
    </main>
  );
}
